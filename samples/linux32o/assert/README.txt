Simple application that compiles to an statically linked executable.
Uses syscalls instead of libc.

Requirements:
- Oberon-07 compiler with linux32o support (compilerX).
- ld linker.

Building:
- Use "./build.sh" script.
