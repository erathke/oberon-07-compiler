MODULE Coroutines;
IMPORT SYSTEM, Out := Out_sys, TR := Traps;

CONST 
	IDLE* = 0;
	RUNNING* = 1;
	SUSPENDED* = 2;
	DEAD* = 4;
	
	DEFAULT_STACK_SIZE* = 1024;
	N_REGS = 6;
	
	HANDLER_ERR = 30;
	
TYPE
	UINT32 = INTEGER;
	
	Userdata* = POINTER TO RECORD
	END;
	
	Context* = POINTER TO ContextDesc;
	
	Handler* = PROCEDURE (ctx: Context);
	
	
	Stack = POINTER TO RECORD
		data: UINT32;
		size: INTEGER
	END;
	
	JmpBuf = ARRAY N_REGS OF UINT32;
	
	ContextDesc = RECORD
		csp: UINT32;
		stack: Stack;
		buf: JmpBuf;
		handler: Handler;
		trap: TR.Trap;
		status: INTEGER;
		udata*: Userdata;
		errorCode*: INTEGER
	END;
	

PROCEDURE [extern, "coroutine_setup"] coro_setup(deadStatus: INTEGER);

PROCEDURE [extern, "coroutine_init32"] coro_init(ctx: UINT32; handlerAddr: UINT32; stackAddr: UINT32);

PROCEDURE [extern, "coroutine_yield32"] coro_yield(ctx: UINT32; data: UINT32): INTEGER;

PROCEDURE [extern, "coroutine_yield32"] coro_resume(ctx: UINT32; data: UINT32): INTEGER;

PROCEDURE [extern, "setjmp"] setjmp(cpu: UINT32): INTEGER;

PROCEDURE [extern, "longjmp"] longjmp(cpu: UINT32; code: INTEGER): INTEGER;

PROCEDURE [extern:stdcall, "_exit_thread"] _exit_thread(code: INTEGER);

PROCEDURE [extern:stdcall, "_new"] _new(t, size: INTEGER; VAR ptr: INTEGER);


PROCEDURE Error(err: INTEGER);
BEGIN
	CASE err OF
	| HANDLER_ERR: 
		Out.String("Invalid handler: NIL"); Out.Ln;
		_exit_thread(HANDLER_ERR);
	END;
END Error;


PROCEDURE JmpTrap(code: INTEGER; context: TR.Context);
VAR
	bufAddr: UINT32;
	ctx: Context;
BEGIN
	ctx := SYSTEM.VAL(context, Context);
	ctx.errorCode := code;
	bufAddr := SYSTEM.ADR(ctx.buf);
	code := longjmp(bufAddr, 1);
END JmpTrap;


PROCEDURE [ccall] EntryPoint(ctxAddr: UINT32);
VAR
	ctx: Context;
BEGIN
	ctx := SYSTEM.VAL(ctxAddr, Context);
	ctx.handler(ctx);
END;


PROCEDURE Create*(handler: Handler; data: Userdata; stackSize: INTEGER): Context;
CONST 
	NULL = 0;
	TYPE_OFFSET = 16;
VAR
	ctx: Context;
	stack: Stack;
	spAddr, entryAddr, stackPtr: UINT32;
BEGIN
	IF handler = NIL THEN Error(HANDLER_ERR) END;
	IF stackSize = 0 THEN stackSize := DEFAULT_STACK_SIZE END;
	
	_new(0, stackSize, stackPtr); // has a type offset
	ASSERT(stackPtr # NULL);
	
	NEW(stack);
	stack.data := stackPtr;
	stack.size := stackSize;
	spAddr := (stackPtr + stack.size) - SYSTEM.SIZE(ContextDesc) - TYPE_OFFSET;
	entryAddr := SYSTEM.ADR(EntryPoint);
	
	ctx := SYSTEM.VAL(spAddr, Context);
	ctx.csp := spAddr;
	ctx.stack := stack;
	ctx.handler := handler;
	ctx.udata := data;
	ctx.trap := TR.NewTrap(spAddr, JmpTrap);
	
	coro_init(spAddr, entryAddr, spAddr);
	
	RETURN ctx
END Create;


PROCEDURE [oberon-] Yield*(ctx: Context): INTEGER;
VAR
	addr: UINT32;
BEGIN
	IF ctx.status = RUNNING THEN
		addr := SYSTEM.VAL(ctx, UINT32);
		ctx.status := coro_yield(addr, SUSPENDED);
	ELSE
		CASE ctx.status OF
		| DEAD: Out.String("cannot yield dead coroutine"); Out.Ln;
		| IDLE: Out.String("coroutine not running"); Out.Ln;
		END;
	END;
	
	RETURN ctx.status
END Yield;


PROCEDURE [ccall] PResume(ctx: Context);
VAR
	bufAddr, ctxAddr: UINT32;
BEGIN
	ctxAddr := SYSTEM.VAL(ctx, UINT32);
	TR.SetTrap(ctx.trap);
	
	bufAddr := SYSTEM.ADR(ctx.buf);
	IF setjmp(bufAddr) = 0 THEN
		ctx.status := coro_resume(ctxAddr, ctx.status);
	ELSE
		ctx.status := DEAD;
	END;
	
	TR.SetTrap(NIL);
END PResume;


PROCEDURE [oberon-] Resume*(ctx: Context): INTEGER;
BEGIN
	IF (ctx.status = IDLE) OR (ctx.status = SUSPENDED) THEN
		ctx.status := RUNNING;
		PResume(ctx);
	ELSE
		CASE ctx.status OF
		| DEAD: Out.String("cannot resume dead coroutine"); Out.Ln;
		END;
	END;
	
	RETURN ctx.status
END Resume;


PROCEDURE Status*(ctx: Context): INTEGER;
BEGIN
	RETURN ctx.status
END Status;


BEGIN
	coro_setup(DEAD);
END Coroutines.
