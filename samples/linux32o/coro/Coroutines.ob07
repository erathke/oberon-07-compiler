MODULE Coroutines;
IMPORT SYSTEM, Out := Out_sys, API := API_sys;

CONST 
	IDLE* = 0;
	RUNNING* = 1;
	SUSPENDED* = 2;
	DEAD* = 4;
	
	STACK_SIZE = 4096;
	N_REGS = 6;
	THREAD_DEPTH = 10;
	
TYPE
	UINT32 = INTEGER;
	
	Userdata* = POINTER TO RECORD
	END;
	
	Context* = POINTER TO ContextDesc;
	
	Handler* = PROCEDURE (ctx: Context; r: INTEGER): INTEGER;
	
	Stack = POINTER TO RECORD
		data: ARRAY STACK_SIZE OF BYTE;
		size: INTEGER
	END;
	
	JmpBuf = ARRAY N_REGS OF UINT32;
	
	ContextDesc = RECORD
		csp: UINT32;
		stack: Stack;
		buf: JmpBuf;
		handler: Handler;
		user*: Userdata;
		status: INTEGER;
		errorCode*: INTEGER
	END;
	
	Thread = RECORD
		ctxs: ARRAY THREAD_DEPTH OF Context;
		size: INTEGER
	END;

VAR
	thread: Thread; // TODO: user thread


PROCEDURE [extern, "coroutine_init32"] coro_init(ctx: UINT32; handlerAddr: UINT32; stackAddr: UINT32);
PROCEDURE [extern, "coroutine_yield32"] coro_yield(ctx: UINT32; data: UINT32): INTEGER;
PROCEDURE [extern, "coroutine_yield32"] coro_resume(ctx: UINT32; data: UINT32): INTEGER;
PROCEDURE [extern, "setjmp"] setjmp(cpu: UINT32): INTEGER;
PROCEDURE [extern, "longjmp"] longjmp(cpu: UINT32; code: INTEGER): INTEGER;


PROCEDURE PushCtx(ctx: Context);
BEGIN
	// TODO: check depth
	thread.ctxs[thread.size] := ctx;
	INC(thread.size);
END PushCtx;


PROCEDURE [oberon-] PopCtx(): Context;
VAR
	ctx: Context;
BEGIN
	ctx := thread.ctxs[thread.size];
	DEC(thread.size);
	RETURN ctx
END PopCtx;


PROCEDURE CurrentCtx(): Context;
BEGIN
	RETURN thread.ctxs[thread.size - 1]
END CurrentCtx;


PROCEDURE [ccall] EntryPoint(ctxAddr: UINT32; r: INTEGER): INTEGER;
VAR
	ctx: Context;
	value: INTEGER;
BEGIN
	ctx := SYSTEM.VAL(ctxAddr, Context);
	value := ctx.handler(ctx, r);
	ctx.status := DEAD;
	RETURN value
END;


PROCEDURE Create*(handler: Handler; data: Userdata): Context;
VAR
	ctx: Context;
	stack: Stack;
	baseAddr, spAddr, spIdx : UINT32;
	handlerAddr: UINT32;
BEGIN
	NEW(stack);
	stack.size := STACK_SIZE;
	baseAddr := SYSTEM.ADR(stack.data[0]);
	spIdx := stack.size - SYSTEM.SIZE(ContextDesc);
	spAddr := SYSTEM.ADR(stack.data[spIdx]);
	handlerAddr := SYSTEM.ADR(EntryPoint);
	
	ctx := SYSTEM.VAL(spAddr, Context);
	ctx.csp := spAddr;
	ctx.stack := stack;
	ctx.handler := handler;
	ctx.user := data;
	
	coro_init(spAddr, handlerAddr, spAddr);
	
	RETURN ctx
END Create;


PROCEDURE [oberon-] Yield*(ctx: Context; r: INTEGER): INTEGER;
VAR
	value: INTEGER;
	addr: UINT32;
BEGIN
	
	IF ctx.status = RUNNING THEN
		addr := SYSTEM.VAL(ctx, UINT32);
		ctx.status := SUSPENDED;
		value := coro_yield(addr, r);
	ELSE
		CASE ctx.status OF
		| DEAD: Out.String("cannot yield dead coroutine"); Out.Ln;
		| IDLE: Out.String("coroutine not running"); Out.Ln;
		END;
	END;
	
	RETURN value
END Yield;


PROCEDURE JmpTrap(code: INTEGER);
VAR
	addr: UINT32;
	currentCtx: Context;
BEGIN
	currentCtx := CurrentCtx();
	currentCtx.errorCode := code;
	addr := SYSTEM.ADR(currentCtx.buf);
	code := longjmp(addr, code);
END JmpTrap;


PROCEDURE [ccall] PResume(ctx: Context; r: INTEGER): INTEGER;
VAR
	value: INTEGER;
	bufAddr, ctxAddr: UINT32;
BEGIN
	API.SetTrap(JmpTrap);
	PushCtx(ctx);
	
	bufAddr := SYSTEM.ADR(ctx.buf);
	IF setjmp(bufAddr) = 0 THEN
		ctxAddr := SYSTEM.VAL(ctx, UINT32);
		value := coro_resume(ctxAddr, r);
	ELSE
		ctx.status := DEAD;
	END;
	
	PopCtx();
	API.SetTrap(NIL);
	RETURN value
	
END PResume;


PROCEDURE [oberon-] Resume*(ctx: Context; r: INTEGER): INTEGER;
VAR
	value: INTEGER;
BEGIN
	IF (ctx.status = IDLE) OR (ctx.status = SUSPENDED) THEN
		ctx.status := RUNNING;
		value := PResume(ctx, r);
	ELSE
		CASE ctx.status OF
		| DEAD: Out.String("cannot resume dead coroutine"); Out.Ln;
		END;
	END;
	
	RETURN value
END Resume;


PROCEDURE Status*(ctx: Context): INTEGER;
BEGIN
	RETURN ctx.status
END Status;


END Coroutines.
